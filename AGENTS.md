# AI 开发助手项目规则

## 1. 沟通方式

我是纯小白，请用简单语言解释，不要默认我懂编程。

如果必须提到技术名词，请先用一句通俗的话解释它是做什么的。

## 2. 工作节奏

每次只做一个小任务。

开始开发前，先确认当前处于哪个阶段：

- MVP 0：假数据 UI 原型。
- MVP 1A：真实核心流程。
- MVP 1B：Word/PDF 导出增强。

不要跳阶段做事。

## 3. 需求边界

不要擅自扩展需求。

第一版明确不做：

- 登录。
- 多用户。
- 手机 App。
- 自动识别真实姓名。
- 完全离线。
- 公司固定 Word/PDF 模板。

如果发现某个功能可能超出范围，先说明风险，再等待确认。

## 4. 开源复用原则

正式从零开发前，优先查找是否有成熟 GitHub 开源项目可以参考或复用。

重点参考：

- 会议录音转写。
- 会议纪要生成。
- 说话人区分。
- 本地历史记录保存。
- Word/PDF 导出。

不能盲目照搬。需要检查：

- 许可证是否允许使用。
- 项目是否还在维护。
- 技术栈是否匹配。
- 复杂度是否适合第一版。
- 适配成本是否合理。

## 5. UI 风格原则

UI 风格参考 Notion / Linear / Apple Notes。

第一版不追求复杂设计稿，但要做到：

- 现代。
- 干净。
- 专业。
- 左侧历史记录栏。
- 右侧主工作区。
- 卡片式布局。
- 清晰按钮。
- 明确加载状态。
- 明确空状态。

不要做花哨动画。

## 6. MVP 阶段规则

MVP 0 先用假数据。

目标是确认页面流程，不接真实 API。

MVP 1A 再接真实 API。

目标是完成：

- 上传真实音频。
- 阿里云百炼 Fun-ASR 录音文件识别。
- 默认语音转写模型：fun-asr。
- 原 qwen3-asr-flash-filetrans 适合长音频识别，但不支持说话人分离，不作为当前默认语音转写方案。
- Fun-ASR 支持 diarization_enabled，开启后结果中可返回 speaker_id，用于映射为“发言人1、发言人2”。
- 发言人编号区分。
- 用户修改转写文字。
- 阿里云百炼 Qwen 文本生成会议纪要和报告提纲。
- 用户修改会议纪要和报告提纲。
- SQLite 保存本机历史记录。
- 历史记录自动保存。
- 自动保存触发时机：上传录音、转写完成、生成纪要、编辑转写文本、编辑会议纪要、编辑报告提纲。
- 自动保存状态文案：正在保存... / 已自动保存 / 自动保存失败，请点击手动保存。
- “保存到本机”按钮保留，作为手动立即保存按钮。
- 支持单条删除历史记录，删除前需要二次确认。
- 支持批量删除历史记录。
- 支持一次选择多个音频，每个音频创建独立任务；上传并发最多 2 个，单个失败不能影响其他任务。
- 复制结果。

MVP 1B 再做 Word/PDF 导出。

Word/PDF 不进入 MVP 1A。

批量删除历史记录已进入当前演示版，交互要简单清楚，不做复杂权限管理。

## 7. 推荐技术方案

当前推荐采用“方案 A：阿里云百炼正式方案”：

- Next.js。
- 阿里云百炼 Fun-ASR / fun-asr。
- 阿里云百炼 Qwen 文本生成。
- SQLite。

客户在中国大陆，当前默认正式方案统一采用阿里云百炼体系。原因是中文普通话、30 分钟以上录音、国内账号付款、访问、发票和交付更方便。

客户明确需要“发言人1/发言人2”区分，因此默认 ASR 使用 Fun-ASR，并开启 diarization_enabled。speaker_count 默认留空，让 Fun-ASR 自动判断实际说话人数；只有明确知道人数时才临时设置，不能作为固定默认值。

OSS 前端直传是当前主上传方案，必须使用客户自己的 OSS 配置。继续使用 OSS_ENABLED、OSS_REGION、OSS_BUCKET 等环境变量，不要写死杭州、新加坡或任何具体 Bucket。阿里云百炼临时上传保留为 fallback，但不要作为客户界面的主方案展示。

长音频体验规则：

- 客户界面暂时只保留“智能会议转写/标准会议模式”，默认开启说话人分离。
- 不再展示“快速转写模式”作为主入口。
- 页面只做轻提示：长音频处理时间会受网络和录音质量影响，请保持页面打开直到任务提交成功；任务提交后，可稍后在历史记录中查看结果。
- MVP 1A 暂时用 localStorage 保存异步任务状态和历史记录，后续 SQLite 阶段再做正式持久化。
- 默认转写结果保留 30 天，原始音频建议通过客户 OSS 生命周期规则 7 天后清理。

AssemblyAI 改为海外备选，不作为国内客户默认方案。

OpenAI 改为海外备选，不作为国内客户默认方案。

DeepSeek 改为低成本备选，不作为当前默认方案。

后续代码应封装 Speech Provider 和 LLM Provider，不要把语音转写供应商或大模型供应商写死在页面组件里。当前默认 Speech Provider 是 aliyun，当前默认 LLM Provider 是 qwen。

## 8. 每步完成后的说明

每完成一步，都要说明：

- 修改了哪些文件。
- 做了什么。
- 如何运行。
- 如何验收。

如果当前步骤只是写文档，也要明确说明没有写代码、没有安装依赖。
