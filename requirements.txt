# AI 会议助手 — Python 依赖（核心包）
# 用法：pip install -r requirements.txt
#
# 注意：torch 安装取决于平台，setup 脚本会自动处理。

# ── 核心 ML 包 ────────────────────────────────────────────────────────────────
funasr>=1.0
modelscope>=1.9
transformers>=4.35
sentencepiece>=0.1.99
accelerate>=0.26

# ── 说话人识别 ────────────────────────────────────────────────────────────────
pyannote.audio>=3.1

# ── 音频处理 ──────────────────────────────────────────────────────────────────
soundfile>=0.12
librosa>=0.10

# ── 其他工具 ──────────────────────────────────────────────────────────────────
onnxruntime>=1.16
huggingface_hub>=0.19
datasets>=2.14
einops>=0.7
